<%= form_for(@tabla) do |f| %>
  <% if @tabla.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tabla.errors.count, "error") %> prohibited this tabla from being saved:</h2>

      <ul>
      <% @tabla.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :titulo %><br>
    <%= f.text_field :titulo %>
  </div>
  <div class="field">
    <%= f.label :categoria %><br>
    <%= f.text_field :categoria %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
